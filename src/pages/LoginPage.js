import react from "react";

import {
    LoginForm
} from "@/components";

function LoginPage({props}){

    return(
        <div>
            <LoginForm></LoginForm>
        </div>
    )
}

export default LoginPage